/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Build tests"
                dir ("c:\\apache-maven-3.8.6\\bin\\") {
                    echo "Execute maven"
                }

            }
        }
    }
}